Phing based installer for a laravel application

Installation:

composer install

Example:

cd build
php ../vendor/phing/phing/bin/phing.php -f build.xml -DdirName=yourDirName -DwebsiteUrl=example.com
